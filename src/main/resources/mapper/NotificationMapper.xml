<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.music.mapper.NotificationMapper">

    <!-- 实体映射 -->
    <resultMap id="NotificationMap" type="com.example.music.entity.Notification">
        <id property="id" column="id"/>
        <result property="from" column="`from`"/>
        <result property="to" column="`to`"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="operation" column="operation"/>
        <result property="content" column="content"/>
        <result property="operationTime" column="operation_time"/>
    </resultMap>

    <!-- 添加通知 -->
    <insert id="addNotification">

        INSERT INTO notification
        (`from`,`to`,target_type,target_id,operation,content,operation_time)
        VALUES
            (#{from},
             #{to},
             #{targetType},
             #{targetId},
             #{operation},
             #{content},
             now())

    </insert>

    <!-- 查询某个用户收到的通知 -->
    <select id="queryByTo" resultMap="NotificationMap">
        SELECT *
        FROM notification
        WHERE `to` = #{to}
        ORDER BY operation_time DESC
            LIMIT #{start},#{size}
    </select>

    <!-- 删除通知 -->
    <delete id="deleteNotification">
        DELETE
        FROM notification
        WHERE id = #{id}
    </delete>

</mapper>